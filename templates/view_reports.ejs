<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Reports - Luapula Supply Chain</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="{{ url_for('static', filename='css/styles.css') }}" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="mt-5">View Reports</h1>
        <a href="{{ url_for('dashboard') }}" class="btn btn-secondary mb-3">Back to Dashboard</a>
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }}">{{ message }}</div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Facility</th>
                    <th>Date</th>
                    <th>Period</th>
                </tr>
            </thead>
            <tbody>
                {% for report in reports.items %}
                    <tr>
                        <td>{{ report.id }}</td>
                        <td>{{ report.facility.name }}</td>
                        <td>{{ report.report_date }}</td>
                        <td>{{ report.report_period }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        {% if reports.has_prev or reports.has_next %}
            <nav aria-label="Page navigation">
                <ul class="pagination">
                    {% if reports.has_prev %}
                        <li class="page-item"><a class="page-link" href="{{ url_for('view_reports', page=reports.prev_num) }}">Previous</a></li>
                    {% endif %}
                    {% if reports.has_next %}
                        <li class="page-item"><a class="page-link" href="{{ url_for('view_reports', page=reports.next_num) }}">Next</a></li>
                    {% endif %}
                </ul>
            </nav>
        {% endif %}
    </div>
</body>
</html>
